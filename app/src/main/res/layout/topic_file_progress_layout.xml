<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
       xmlns:tools="http://schemas.android.com/tools"
       android:layout_width="wrap_content"
       android:layout_height="wrap_content">

    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="@dimen/dashboard_icon_size"
        android:layout_gravity="center_horizontal"
        android:layout_marginStart="@dimen/medium_margin">

        <ImageView
            android:id="@+id/button_action_file"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/dashboard_icon_size"
            android:layout_gravity="center"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:scaleType="center"
            tools:ignore="ContentDescription"
            tools:src="@drawable/ic_file_download"/>

        <ProgressBar
            android:id="@+id/audio_download_progress"
            android:layout_width="@dimen/list_progress_size"
            android:layout_height="@dimen/list_progress_size"
            android:layout_gravity="center"
            android:gravity="center"
            android:indeterminate="true"
            android:maxHeight="@dimen/list_progress_size"
            android:maxWidth="@dimen/list_progress_size"
            android:visibility="invisible"/>
    </FrameLayout>
</merge>