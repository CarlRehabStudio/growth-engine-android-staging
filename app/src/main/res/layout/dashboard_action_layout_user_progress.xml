<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <FrameLayout
        android:id="@+id/user_progress_container"
        style="?attr/actionButtonStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/user_progress_icon"
            android:layout_width="@dimen/dashboard_user_progress_toolbar_icon_width"
            android:layout_height="@dimen/dashboard_user_progress_toolbar_icon_height"
            android:layout_gravity="center"
            android:src="@drawable/ic_badge"/>

        <TextView
            android:id="@+id/label_progress"
            style="@style/FontCaption"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:textColor="@color/white"
            tools:text="100"/>
    </FrameLayout>
</FrameLayout>